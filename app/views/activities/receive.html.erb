<div class="container">
  <div class="card">
    <div class="card-block">
      <h1 class="card-title"><%= @activity.track.title %></h1>
      <h4 class="card-title"><%= @activity.title %></h4>
        <p class="card-text">
          <div class="card">
            <div class="card-header">Receive</div>
            <div class="card-block">
              <p class="card-text" style="white-space: pre-line;" ><%= @activity.receive %></p>
            </div>
          </div>
        </p>
      <%= link_to 'Journal', root_url %>  |
      <%= link_to 'Back to Group', group_posts_path(@group) %>  |
      <%= link_to 'Next', reflect_group_track_activity_path(@group) %>
    </div> 
  </div> 
</div>
