<div class="container-fluid">
  <div class="card">
    <div class="card-block">
      <h1 class="card-title">
        <%= @activity.track.title %>
        <h4><%= @activity.track.description %></h4>
      </h1>

<div class="container-fluid">
<div class="card" style="max-width: 20rem;">

  <div class="card-block">
    <h4 class="card-title"><%= @activity.title %></h4>
      <p class="card-text"><%= @activity.receive %></p>
<!-- Links -->
       
  </div>
 <div>
</div>
</div>
<progress class="progress" style="max-width: 20rem;" value="5" max="100">5%</progress>

<%= link_to 'Back', track_path(@activity.track, @activity) %>  |
<%= link_to 'Home', root_url %>  |
<%= link_to 'Next', reflect_track_activity_path %>

  </div>
 </div>
</div>